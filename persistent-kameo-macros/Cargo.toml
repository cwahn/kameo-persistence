[package]
name = "persistent-kameo-macros"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1.0"
quote = "1.0"
syn = { version = "2.0", features = ["full", "extra-traits"] }

# Optional: for better error messages
proc-macro-error = "1.0"
heck = "0.5.0"
